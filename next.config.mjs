// import type { NextConfig } from 'next'

const nextConfig = {
  /* config options here */
  images: {
    domains: [
      'search.pstatic.net',
      'example.com',
      'imgnews.naver.net',
      'lh3.googleusercontent.com',
    ],
  },
}

export default nextConfig
